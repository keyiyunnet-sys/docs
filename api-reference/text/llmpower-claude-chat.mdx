---
title: "Claude Messages API"
description: "基于 Claude Messages 协议的对话接口，适合使用 Anthropic 原生格式接入"
api: "POST http://apillm.globalaiopc.com/gw_llm_power/v1/messages"
---

## 认证

使用 `Authorization` 请求头进行身份认证。

```bash
Authorization: Bearer {{YOUR_API_KEY}}
Content-Type: application/json
```

说明：

- `Authorization`：API 密钥，格式为 `Bearer {{YOUR_API_KEY}}`

## 快速导入 CC Switch

<Accordion title="导入 CC Switch">
  <label style={{ display: 'grid', gap: '6px', marginBottom: '12px' }}>
    <span>API Key</span>
    <input id="ccswitch-api-key" type="password" placeholder="请输入你的 API Key" style={{ padding: '10px 12px', border: '1px solid #d1d5db', borderRadius: '8px' }} />
  </label>

  导入后将使用以下模型配置：
  - 主模型：`gpt-5.4-low`
  - 备选模型：`gpt-5.5-low`

  <button
    type="button"
    onClick={() => {
      const apiKey = document.getElementById('ccswitch-api-key')?.value?.trim();

      if (!apiKey) {
        window.alert('请输入 API Key。');
        return;
      }

      const params = new URLSearchParams({
        resource: 'provider',
        app: 'claude',
        name: '客易云LLM (gpt-5.4-low)',
        endpoint: 'http://apillm.globalaiopc.com/gw_llm_power',
        apiKey,
        model: 'gpt-5.4-low',
        haikuModel: 'gpt-5.5-low',
        sonnetModel: 'gpt-5.4-low',
        opusModel: 'gpt-5.4-low',
        homepage: 'http://apillm.globalaiopc.com/gw_llm_power',
        enabled: 'true',
      });
      window.location.href = `ccswitch://v1/import?${params.toString()}`;
    }}
    style={{ display: 'inline-block', padding: '10px 16px', borderRadius: '8px', background: '#2563eb', color: '#ffffff', fontWeight: 600, border: 'none', cursor: 'pointer' }}
  >
    确定
  </button>
</Accordion>

<Note>
点击“导入 CC Switch”后，可在页面内填写 API Key，并通过 4 个下拉框选择模型。默认值优先使用 `gpt-5.4-low`，`-cc` 后缀模型仅限 CC Switch 使用。
</Note>

## 支持的模型

当前支持以下 Claude 模型：

- `claude-haiku-4-5`：Claude Haiku 4.5 满血版本
- `claude-haiku-4-5-fd`：Claude Haiku 4.5 kiro 代理
- `claude-haiku-4-5-hc`：Claude Haiku 4.5 AWS版本
- `claude-opus-4-5`：Claude Opus 4.5 满血版本
- `claude-opus-4-5-fd`：Claude Opus 4.5 kiro 代理
- `claude-opus-4-5-hc`：Claude Opus 4.5 AWS版本
- `claude-opus-4-6`：Claude Opus 4.6 满血版本
- `claude-opus-4-6-fd`：Claude Opus 4.6 kiro 代理
- `claude-opus-4-6-hc`：Claude Opus 4.6 AWS版本
- `claude-opus-4-7`：Claude Opus 4.7 满血版本
- `claude-opus-4-7-fd`：Claude Opus 4.7 kiro 代理
- `claude-opus-4-7-hc`：Claude Opus 4.7 AWS版本
- `claude-sonnet-4-5`：Claude Sonnet 4.5 满血版本
- `claude-sonnet-4-5-fd`：Claude Sonnet 4.5 kiro 代理
- `claude-sonnet-4-5-hc`：Claude Sonnet 4.5 AWS版本
- `claude-sonnet-4-6`：Claude Sonnet 4.6 满血版本
- `claude-sonnet-4-6-fd`：Claude Sonnet 4.6 kiro 代理
- `claude-sonnet-4-6-hc`：Claude Sonnet 4.6 AWS版本

### Claude Messages API 兼容的 GPT 模型

- `gpt-5.4`：GPT-5.4 官方版本
- `gpt-5.4-fd`：GPT-5.4 混合号池 - codex 代理
- `gpt-5.4-hc`：GPT-5.4 优质号池-官号
- `gpt-5.4-low`：GPT-5.4 低价版
- `gpt-5.5`：GPT-5.5 官方版本
- `gpt-5.5-fd`：GPT-5.5 混合号池 - codex 代理
- `gpt-5.5-hc`：GPT-5.5 优质号池-官号
- `gpt-5.5-low`：GPT-5.5 低价版

### CC Switch 专用模型

以下模型只能在 CC Switch 中使用，并且只能通过 Claude Messages API 格式调用：

- `claude-haiku-4-5-cc`：Claude Haiku 4.5 CC 代理
- `claude-opus-4-6-cc`：Claude Opus 4.6 CC 代理
- `claude-opus-4-6-high-cc`：Claude Opus 4.6 High CC 代理
- `claude-opus-4-6-low-cc`：Claude Opus 4.6 Low CC 代理
- `claude-opus-4-6-max-cc`：Claude Opus 4.6 Max CC 代理
- `claude-opus-4-6-medium-cc`：Claude Opus 4.6 Medium CC 代理
- `claude-opus-4-6-thinking-cc`：Claude Opus 4.6 Thinking CC 代理
- `claude-opus-4-7-cc`：Claude Opus 4.7 CC 代理
- `claude-sonnet-4-6-cc`：Claude Sonnet 4.6 CC 代理

<Note>
Claude Messages API 使用 Claude 协议请求体，不适用于 Gemini 官方原生格式。Gemini 官方格式请使用 Gemini Chat API。GPT 模型可通过该格式调用，但当前不支持图片识别。
</Note>

### 多模态能力支持

| 模型 | 图片分析 |
| --- | --- |
| `claude-haiku-4-5` | 支持 |
| `claude-haiku-4-5-fd` | 支持 |
| `claude-haiku-4-5-hc` | 支持 |
| `claude-opus-4-5` | 支持 |
| `claude-opus-4-5-fd` | 支持 |
| `claude-opus-4-5-hc` | 支持 |
| `claude-opus-4-6` | 支持 |
| `claude-opus-4-6-fd` | 支持 |
| `claude-opus-4-6-hc` | 支持 |
| `claude-opus-4-7` | 支持 |
| `claude-opus-4-7-fd` | 支持 |
| `claude-opus-4-7-hc` | 支持 |
| `claude-sonnet-4-5` | 支持 |
| `claude-sonnet-4-5-fd` | 支持 |
| `claude-sonnet-4-5-hc` | 支持 |
| `claude-sonnet-4-6` | 支持 |
| `claude-sonnet-4-6-fd` | 支持 |
| `claude-sonnet-4-6-hc` | 支持 |
| `gpt-5.4` | 不支持 |
| `gpt-5.4-fd` | 不支持 |
| `gpt-5.4-hc` | 不支持 |
| `gpt-5.4-low` | 不支持 |
| `gpt-5.5` | 不支持 |
| `gpt-5.5-fd` | 不支持 |
| `gpt-5.5-hc` | 不支持 |
| `gpt-5.5-low` | 不支持 |
| `claude-haiku-4-5-cc` | 支持 |
| `claude-opus-4-6-cc` | 支持 |
| `claude-opus-4-6-high-cc` | 支持 |
| `claude-opus-4-6-low-cc` | 支持 |
| `claude-opus-4-6-max-cc` | 支持 |
| `claude-opus-4-6-medium-cc` | 支持 |
| `claude-opus-4-6-thinking-cc` | 支持 |
| `claude-opus-4-7-cc` | 支持 |
| `claude-sonnet-4-6-cc` | 支持 |

## 请求参数

<ParamField path="model" type="string" required>
  要使用的模型名称，例如：`claude-opus-4-7`、`gpt-5.5`、`claude-opus-4-6-thinking-cc`。`-cc` 后缀模型仅限 CC Switch 使用
</ParamField>

<ParamField path="max_tokens" type="integer" required>
  本次请求允许生成的最大 token 数
</ParamField>

<ParamField path="messages" type="array" required>
  Claude Messages 协议的消息数组
</ParamField>

<ParamField path="messages[].role" type="string" required>
  消息角色，常见值为 `user`、`assistant`
</ParamField>

<ParamField path="messages[].content" type="string/array" required>
  消息内容。可以是字符串，也可以是 Claude 原生内容块数组
</ParamField>

<ParamField path="system" type="string">
  系统提示词，可选
</ParamField>

<ParamField path="stream" type="boolean">
  是否启用流式响应。开启后返回 Claude 风格流式事件
</ParamField>

<ParamField path="temperature" type="number">
  采样温度
</ParamField>

## 响应说明

接口返回 Claude Messages 风格响应。

<ResponseField name="id" type="string">
  响应唯一标识
</ResponseField>

<ResponseField name="type" type="string">
  响应类型，通常为 `message`
</ResponseField>

<ResponseField name="role" type="string">
  返回消息角色，通常为 `assistant`
</ResponseField>

<ResponseField name="content[].type" type="string">
  内容块类型，常见为 `text`
</ResponseField>

<ResponseField name="content[].text" type="string">
  模型生成的文本内容
</ResponseField>

<ResponseField name="stop_reason" type="string">
  生成结束原因
</ResponseField>

<ResponseField name="usage.input_tokens" type="integer">
  输入 token 数
</ResponseField>

<ResponseField name="usage.output_tokens" type="integer">
  输出 token 数
</ResponseField>

## 注意事项

<Note>
1. Claude 格式请求路径为 `POST /v1/messages`
2. 流式场景请在请求体中传递 `"stream": true`
3. 请使用 `Authorization` 请求头传递 API 密钥
</Note>

<RequestExample>

```bash cURL (纯文本)
curl --request POST \
  --url http://apillm.globalaiopc.com/gw_llm_power/v1/messages \
  --header 'Authorization: Bearer {{YOUR_API_KEY}}' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "claude-opus-4-6",
    "max_tokens": 1024,
    "system": "你是一个专业写作助手",
    "messages": [
      {
        "role": "user",
        "content": "请帮我写一段产品介绍"
      }
    ]
  }'
```

```bash cURL (图片分析)
curl --request POST \
  --url http://apillm.globalaiopc.com/gw_llm_power/v1/messages \
  --header 'Authorization: Bearer {{YOUR_API_KEY}}' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "claude-opus-4-6",
    "max_tokens": 1024,
    "system": "你是一个图片分析专家。",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "text",
            "text": "请分析这张图片的内容"
          },
          {
            "type": "image",
            "source": {
              "type": "url",
              "url": "https://example.com/image.jpg"
            }
          }
        ]
      }
    ]
  }'
```

```python Python
import requests

response = requests.post(
    'http://apillm.globalaiopc.com/gw_llm_power/v1/messages',
    headers={
        'Authorization': 'Bearer {{YOUR_API_KEY}}',
        'Content-Type': 'application/json'
    },
    json={
        'model': 'claude-opus-4-6',
        'max_tokens': 1024,
        'system': '你是一个专业写作助手',
        'messages': [
            {
                'role': 'user',
                'content': '请帮我写一段产品介绍'
            }
        ]
    }
)

print(response.json())
```

</RequestExample>

<ResponseExample>

```json 非流式响应
{
  "id": "msg_01ABC123",
  "type": "message",
  "role": "assistant",
  "content": [
    {
      "type": "text",
      "text": "当然可以，下面是一段简洁的产品介绍文案。"
    }
  ],
  "model": "claude-opus-4-6",
  "stop_reason": "end_turn",
  "usage": {
    "input_tokens": 18,
    "output_tokens": 32
  }
}
```

```json 流式响应
event: message_start
data: {"type":"message_start","message":{"id":"msg_01ABC123","type":"message","role":"assistant","model":"claude-opus-4-6","content":[]}}

event: content_block_delta
data: {"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"当然可以"}}

event: message_stop
data: {"type":"message_stop"}
```

</ResponseExample>
