---
title: "llmpower OpenAI Chat API"
description: "基于 OpenAI 兼容格式的 llmpower 对话接口，适合使用 Chat Completions 协议接入"
api: "POST http://apillm.globalaiopc.com/gw_llm_power/v1/chat/completions"
---

## 认证

llmpower 对外接口主要使用 `X-User-Id` 模式进行身份识别。

请求头要求：

```bash
X-User-Id: {{YOUR_USER_ID}}
X-Platform: {{YOUR_PLATFORM}}
X-Signature: {{YOUR_SIGNATURE}}
Content-Type: application/json
```

说明：

- `X-User-Id`：业务用户 ID，必传
- `X-Platform`：调用平台标识，建议显式传递
- `X-Signature`：签名值，计算规则为 `hex(HMAC_SHA256(secret, platform + userId))`

## 请求参数

<ParamField path="model" type="string" required>
  要使用的模型名称，例如：`gpt-4.1`、`gemini-2.5-pro`、`claude-3-7-sonnet`
</ParamField>

<ParamField path="messages" type="array" required>
  对话消息数组。每个消息对象通常包含 `role` 和 `content` 字段
</ParamField>

<ParamField path="temperature" type="number">
  采样温度，控制输出的随机性。建议范围为 0 到 2
</ParamField>

<ParamField path="top_p" type="number">
  核采样参数。建议与 `temperature` 二选一调整
</ParamField>

<ParamField path="stream" type="boolean">
  是否启用流式返回。设置为 `true` 时，响应将以 SSE 格式返回，并以 `data: [DONE]` 结束
</ParamField>

<ParamField path="max_completion_tokens" type="integer">
  模型最大输出 token 数
</ParamField>

<ParamField path="stop" type="string/array">
  停止生成的序列，可传字符串或数组
</ParamField>

### Messages 数组结构

<ParamField path="role" type="string" required>
  消息角色，可选值：`system`、`user`、`assistant`
</ParamField>

<ParamField path="content" type="string/array" required>
  消息内容。可以是纯文本字符串，也可以是 OpenAI 兼容的多模态数组
</ParamField>

## 响应说明

接口返回 OpenAI 兼容格式响应。

<ResponseField name="id" type="string">
  本次对话请求的唯一标识
</ResponseField>

<ResponseField name="object" type="string">
  返回对象类型，通常为 `chat.completion`
</ResponseField>

<ResponseField name="created" type="integer">
  响应创建时间戳
</ResponseField>

<ResponseField name="model" type="string">
  实际使用的模型名称
</ResponseField>

<ResponseField name="choices[].message.role" type="string">
  模型返回消息角色，通常为 `assistant`
</ResponseField>

<ResponseField name="choices[].message.content" type="string">
  模型生成的文本内容
</ResponseField>

<ResponseField name="usage.prompt_tokens" type="integer">
  输入 token 数
</ResponseField>

<ResponseField name="usage.completion_tokens" type="integer">
  输出 token 数
</ResponseField>

<ResponseField name="usage.total_tokens" type="integer">
  总 token 数
</ResponseField>

## 注意事项

<Note>
1. 请求地址固定为 `http://apillm.globalaiopc.com/gw_llm_power`
2. 只要传了 `X-User-Id`，就必须传 `X-Signature`
3. `X-Platform` 如果参与签名，请确保与请求头中的值完全一致
4. 流式响应使用 SSE 返回，结束标志为 `data: [DONE]`
</Note>

<RequestExample>

```bash cURL
curl --request POST \
  --url http://apillm.globalaiopc.com/gw_llm_power/v1/chat/completions \
  --header 'X-User-Id: {{YOUR_USER_ID}}' \
  --header 'X-Platform: {{YOUR_PLATFORM}}' \
  --header 'X-Signature: {{YOUR_SIGNATURE}}' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "gpt-4.1",
    "messages": [
      {
        "role": "system",
        "content": "你是一个专业助手"
      },
      {
        "role": "user",
        "content": "请介绍一下你自己"
      }
    ],
    "temperature": 0.7
  }'
```

```python Python
import requests

response = requests.post(
    'http://apillm.globalaiopc.com/gw_llm_power/v1/chat/completions',
    headers={
        'X-User-Id': '{{YOUR_USER_ID}}',
        'X-Platform': '{{YOUR_PLATFORM}}',
        'X-Signature': '{{YOUR_SIGNATURE}}',
        'Content-Type': 'application/json'
    },
    json={
        'model': 'gpt-4.1',
        'messages': [
            {'role': 'system', 'content': '你是一个专业助手'},
            {'role': 'user', 'content': '请介绍一下你自己'}
        ],
        'temperature': 0.7
    }
)

print(response.json())
```

</RequestExample>

<ResponseExample>

```json 非流式响应
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "created": 1761635478,
  "model": "gpt-4.1",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "你好，我是 llmpower 提供的 AI 助手，很高兴为你服务。"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 20,
    "completion_tokens": 18,
    "total_tokens": 38
  }
}
```

```json 流式响应
data: {"id":"chatcmpl-abc123","object":"chat.completion.chunk","created":1761635478,"model":"gpt-4.1","choices":[{"index":0,"delta":{"role":"assistant","content":"你好"},"finish_reason":null}]}

data: {"id":"chatcmpl-abc123","object":"chat.completion.chunk","created":1761635478,"model":"gpt-4.1","choices":[{"index":0,"delta":{"content":"！"},"finish_reason":null}]}

data: [DONE]
```

</ResponseExample>
